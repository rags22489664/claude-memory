# /memory:search

Search your long-term memory using semantic search.

## Usage
```
/memory:search <query>
```

## Instructions

Run the following command to search memories:

```bash
cd ~/.claude-memory && leann search memory "<query>" --top-k 10
```

Display the results to the user, showing the most relevant memories found.
